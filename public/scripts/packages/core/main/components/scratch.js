define(['react', 'lodash', 'core/components/scratch.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'scratch',
        render: template
    });
});
